# 永續小食堂 HTML 投影片

和美高中多元選修「永續小食堂」第一堂課投影片，內容包含課程簡介、升學制度插播、課程規則、分組活動，以及飲食與永續緒論。

## 使用方式

直接開啟 `index.html`，或在資料夾內執行：

```bash
python3 -m http.server 8000
```

再開啟 <http://localhost:8000>。

## 操作

- `←` / `→`、`PageUp` / `PageDown`：前後換頁
- `Space` / `Enter`：下一頁
- `Home` / `End`：第一頁／最後一頁
- `F`：全螢幕
- `N`：講者備註
- 觸控裝置可左右滑動換頁

## 設計與內容說明

- 字體使用 macOS 與常見系統內建繁中文字型，不依賴外部 CDN。
- 視覺以字級、字重與配色變化為主，抽象插畫只用於少數章節轉場。
- 升學制度補充以 115 學年度現行規則為基準；學生實際申請時，仍應查閱當年度簡章。
- 原稿中的教室口語與提問盡量保留，講者提示放在可按 `N` 開啟的備註面板。

## 圖像

`assets/food-system.png` 與 `assets/hidden-costs.png` 由 OpenAI ImageGen 依本課程需求產生，未使用外部圖片素材。

## 授權

課程文字與投影片設計保留所有權利。未經授權，請勿重製或另行散布。
